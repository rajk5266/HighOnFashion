---

// Optional: add Font Awesome CDN for Pinterest & Reddit

const { title, description, date, readTime, cover } = Astro.props;
---

<section class="max-w-3xl mx-auto px-6 py-20">
  <!-- Cover Image -->
  {cover && (
    <img
      src={cover}
      alt={title}
      class="w-full h-64 md:h-96 object-cover rounded-xl mb-12"
    />
  )}

  <!-- Title & Meta -->
  <header class="mb-10">
    <h1 class="text-5xl font-serif mb-4">{title}</h1>
    <p class="text-neutral-500 text-sm">
      {new Date(date).toLocaleDateString()} · {readTime}
    </p>
    {description && (
      <p class="mt-4 text-neutral-700">{description}</p>
    )}
  </header>

  <!-- Blog Content -->
  <article class="prose prose-neutral max-w-none">
    <slot />
  </article>

</section>
